#function counts occurrence of "item" in the list "sequence"
def count(sequence, item):
    total = 0
    for i in range(len(sequence)):
        if sequence[i] == item:
            total +=1
    return total
